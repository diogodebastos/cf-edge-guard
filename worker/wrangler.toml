# This file exists ONLY so `wrangler deploy --dry-run --outdir dist` can bundle
# the TypeScript. Terraform owns the real deploy, the bindings and the routes:
# never run a plain `wrangler deploy` for this Worker.
name = "edge-guard"
main = "src/index.ts"
compatibility_date = "2026-09-01"
